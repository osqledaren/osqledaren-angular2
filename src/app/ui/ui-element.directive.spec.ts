import { UIElementDirective } from './ui-element.directive';

describe('UiElementDirective', () => {
  it('should create an instance', () => {
    const directive = new UIElementDirective();
    expect(directive).toBeTruthy();
  });
});
