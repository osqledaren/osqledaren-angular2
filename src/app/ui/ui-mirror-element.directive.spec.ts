import { UIMirrorElementDirective } from './ui-mirror-element.directive';

describe('UiMirrorElementDirective', () => {
  it('should create an instance', () => {
    const directive = new UIMirrorElementDirective();
    expect(directive).toBeTruthy();
  });
});
